on:
  workflow_call:
    inputs:
      given_input:
        required: true
        type: string
        
jobs:
  generate_string_job:
    runs-on: self-hosted
    steps:
    - uses: mcbillings/composite-actions/first-action/action.yml@v1
      with:
        given_input: ${{ inputs.given_input }}
